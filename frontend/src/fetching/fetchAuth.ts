import {
    loginURL,
    registerURL,
    logoutURL,
    refreshTokenURL,
    changePasswordURL,
    changeUsernameURL,
} from "./urls";

import { 
    requestHeaders,
    loginBody,
    registerBody, 
} from "./requestConstructors"


const fetchLogin = async () => {
    
}