import React from "react";

interface MessageBarProps {
    sendMessageCallable: () => void;
}

const MessageBar = () => {
    return(
        <div>

        </div>
    );
};

export default MessageBar;