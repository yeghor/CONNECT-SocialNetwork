import React from "react";
import {ShortUserProfile} from "../../../fetching/responseDTOs.ts";

interface UserProps {
    data: ShortUserProfile;
}

const FlowUser = (props: any) => {
    return(
        <div className="h-full w-full bg-white">

        </div>
    );
};

export default FlowUser;